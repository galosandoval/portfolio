import styled from "styled-components/macro";
import { Button, Container } from "../../styles/shared";
import { Center } from "../../styles/utility";

export const LandingStyles = styled.section`
  display: flex;
  justify-content: start;
  flex-direction: column;
  align-items: start;
  height: 100vh;
  width: 83%;
  padding-top: 10rem;
`;

export const Header = styled.h1`
  font-size: 3.5rem;
`;
